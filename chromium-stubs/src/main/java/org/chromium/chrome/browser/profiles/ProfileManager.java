package org.chromium.chrome.browser.profiles;

/** Stub for Chromium's ProfileManager. */
public class ProfileManager {
    public static Profile getLastUsedRegularProfile() { return new Profile(); }
}
